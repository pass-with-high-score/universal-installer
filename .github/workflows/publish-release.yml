name: Deploy

on:
  push:
    tags:
      - 'v*'

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: false

jobs:
  deploy:
    name: Build & Deploy to Play Store
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set Version Name
        run: echo "VERSION_NAME=${{ github.ref_name }}" >> $GITHUB_ENV

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'
          bundler-cache: true

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/wrapper
            ~/.gradle/caches
            ~/.gradle/daemon
          key: gradle-${{ runner.os }}-${{ hashFiles('**/*.gradle.kts', '**/libs.versions.toml', 'gradle/wrapper/gradle-wrapper.properties') }}
          restore-keys: |
            gradle-${{ runner.os }}-

      # Signing
      - name: Decode keystore
        run: echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 --decode > app/universal-installer-release.jks

      - name: Create key.properties
        run: |
          cat > key.properties <<EOF
          storeFile=universal-installer-release.jks
          storePassword=${{ secrets.STORE_PASSWORD }}
          keyAlias=${{ secrets.KEY_ALIAS }}
          keyPassword=${{ secrets.KEY_PASSWORD }}
          EOF

      - name: Decode Play Store key
        run: echo "${{ secrets.PLAY_STORE_CONFIG_JSON }}" | base64 --decode > fastlane/play-store-key.json

      # Build & Deploy
      - name: Grant execute permission
        run: chmod +x gradlew

      # Play Store upload is allowed to fail (e.g. metadata/changelog/permission policy
      # rejection) without blocking the GitHub release — we still want the signed artifacts
      # published so users on side-load tracks can grab them.
      - name: Deploy to internal track
        id: play_store
        continue-on-error: true
        run: bundle exec fastlane internal

      # Play got the `store` AAB above. For GitHub we ship the `full` APK — it's the one
      # users want because it has Root support. The store AAB is attached too so power
      # users can rebuild / verify if they care.
      - name: Build full release APK
        run: ./gradlew assembleFullRelease

      # Artifacts
      - name: Upload release AAB & APK
        uses: actions/upload-artifact@v4
        with:
          name: release-build
          path: |
            app/build/outputs/bundle/storeRelease/app-store-release.aab
            app/build/outputs/apk/full/release/app-full-release.apk
          retention-days: 30

      # GitHub release gets the full-flavor APK (with Root). Keeping the store AAB
      # as an extra asset is handy for anyone auditing what Play received.
      - name: Create GitHub Release
        id: create_release
        uses: softprops/action-gh-release@v1
        with:
          name: ${{ env.VERSION_NAME }}
          draft: false
          prerelease: false
          files: |
            app/build/outputs/apk/full/release/app-full-release.apk
            app/build/outputs/bundle/storeRelease/app-store-release.aab
          token: ${{ secrets.GITHUB_TOKEN }}